<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sign In Successful - SafeScroll</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;600&display=swap" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/signin-success.css">
</head>

<body>
    <div class="success-card">
        <div class="success-icon">
            <i class="fas fa-check"></i>
        </div>
        <h1 class="success-title">Welcome Back!</h1>
        <p class="success-message">
            You've successfully signed in to your SafeScroll account. Get ready to experience safer browsing!
        </p>

        <div class="info-sections">
            <div class="access-guide">
                <h2><i class="fas fa-puzzle-piece"></i> Quick Access Guide</h2>
                <p>Find the SafeScroll icon in your browser's extension menu to access all features.</p>
            </div>

            <div class="subscription-info">
                <h2><i class="fas fa-star"></i> Premium Features</h2>
                <p>Unlock advanced protection and exclusive features with a SafeScroll subscription.</p>
                <a href="/subscribe" class="subscribe-btn">
                    <span>Subscribe Now</span>
                    <i class="fas fa-arrow-right"></i>
                </a>
            </div>
        </div>

        <div class="auto-close-note">
            This window will close automatically in a few seconds...
        </div>
    </div>

    <!-- Bootstrap Bundle with Popper -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>

    <script>
        // Auto-close countdown
        let timeLeft = 5;
        const noteElement = document.querySelector('.auto-close-note');
        
        const countdown = setInterval(() => {
            timeLeft--;
            if (timeLeft <= 0) {
                clearInterval(countdown);
                if (window.opener) {
                    window.close();
                }
            } else {
                noteElement.textContent = `This window will close automatically in ${timeLeft} seconds...`;
            }
        }, 1000);

        // Prevent going back
        history.pushState(null, '', window.location.pathname);
        window.onpopstate = function(event) {
            history.go(1);
        };
    </script>
</body>

</html>