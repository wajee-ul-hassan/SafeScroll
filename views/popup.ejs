<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>SafeScroll Settings</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="popup.css">
</head>

<body>
    <div class="container my-1">
        <div id="navbar">
            <span class="logo">
                <img src="./safescroll.png" alt="SafeScroll Logo"> Safescroll
            </span>
            <div>
                <% if (!isLoggedIn) { %>
                    <a class="btn" id="signin" href="/signin">Sign In</a>
                    <% } else { %>
                        <input type="hidden" id="username" value="<%= username %>">
                        <span><svg xmlns="http://www.w3.org/2000/svg" width="30" height="30" fill="currentColor"
                                class="bi bi-person-circle" viewBox="0 0 16 16">
                                <path d="M11 6a3 3 0 1 1-6 0 3 3 0 0 1 6 0" />
                                <path fill-rule="evenodd"
                                    d="M0 8a8 8 0 1 1 16 0A8 8 0 0 1 0 8m8-7a7 7 0 0 0-5.468 11.37C3.242 11.226 4.805 10 8 10s4.757 1.225 5.468 2.37A7 7 0 0 0 8 1" />
                            </svg></span>
                        <% } %>
                            <% if (isSubscribed) { %>
                                <a class="btn" id="dashboard" href="/dashboard">Dashboard</a>
                                <% } %>
            </div>
        </div>
        <div class="form-check form-switch mt-4 check-form">
            <input class="" type="checkbox" id="enableExtensionSwitch">
        </div>
        <% if (!isSubscribed) { %>
            <form id="checkoutForm" action="/subscribe/create-checkout-session" class="text-center mb-3" method="POST">
                <button class="btn btn-primary" type="submit">Subscribe Now</button>
            </form>
            <div class="note">
                <p><b>Note:</b> Buy Subscription to get access to dashboard and view memes blocked by model.</p>
            </div>
            <% } %>
    </div>
    <script src="popup.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>